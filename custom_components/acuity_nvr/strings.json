{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Acuity NVR",
        "description": "Enter the NVR URL. Recommended: the token-gated Scrypted endpoint, e.g. https://scrypted.example.com/endpoint/@acuity/nvr/public (requires the API Token from the plugin's Network settings). Alternative: the standalone server, e.g. https://192.168.1.10:10444 (turn off certificate verification for its self-signed cert).",
        "data": {
          "url": "NVR URL",
          "api_token": "API token (from the plugin's Network settings)",
          "verify_ssl": "Verify SSL certificate"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the NVR. Check the URL and that the plugin is running.",
      "invalid_auth": "The NVR rejected the API token. Check the API Token setting in the plugin.",
      "invalid_url": "Enter a full URL starting with http:// or https://."
    },
    "abort": {
      "already_configured": "This NVR is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Acuity NVR options",
        "description": "Set a Web UI URL to add an Acuity NVR panel to the sidebar (embedded in an iframe). Use the standalone server URL with the token, e.g. https://192.168.1.10:10444/?token=YOURTOKEN — and visit it once directly in your browser to accept the self-signed certificate. Turn off camera or motion entities if a native integration (e.g. Reolink) already provides them; the Media Browser recordings keep working either way.",
        "data": {
          "web_ui_url": "Web UI URL for the sidebar panel (empty = no panel)",
          "create_cameras": "Create camera entities",
          "create_motion_sensors": "Create motion sensors"
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "nvr_motion": {
        "name": "NVR motion"
      }
    }
  }
}
